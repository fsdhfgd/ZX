name = "ZX"
compatibility_date = "2025-05-17"
compatibility_flags = ["nodejs_compat"]

# 关键配置：把当前目录作为静态资源目录
[assets]
directory = "."
binding = "ASSETS"

# 可选：添加一个简单的 Worker 入口（强烈推荐）
main = "src/index.js"
